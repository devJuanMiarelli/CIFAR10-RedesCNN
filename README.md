# CIFAR10-RedesCNN

Algoritmo de processamento de imagem, com modelo salvo, pré-treinado e pré-processado.
Utilizando o banco de Dados CIFAR10 para treinar o modelo e realizar os "predicts". E testando para saber a "accuracy" do algoritmo.
